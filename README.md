# Raporty Wspieram Przyrodę

## Tworzenie i modyfikacja raportów

Instalacja środowiska
```shell
python -m venv .venv
source .venv/bin/activate
pip install pip --upgrade
pip install -r requirements.txt
```

Uruchomienie edytora

```shell
jupyter-lab
```